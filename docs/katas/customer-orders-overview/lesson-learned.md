### Lesson Learned

* ✅ Learned to use `LEFT JOIN` to include unmatched records
* ✅ Sorting results using multiple columns (`ORDER BY first_name, order_details`)
* ✅ Handling `NULL` values in joined tables
* ✅ Practical use case for combining customer and order data
